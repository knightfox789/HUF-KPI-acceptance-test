import fs from 'node:fs/promises';import path from 'node:path';import {spawnSync} from 'node:child_process';import {fileURLToPath} from 'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'../..');async function walk(d){const out=[];for(const e of await fs.readdir(d,{withFileTypes:true})){if(['vendor','_site','node_modules'].includes(e.name)&&e.isDirectory())continue;const p=path.join(d,e.name);if(e.isDirectory())out.push(...await walk(p));else out.push(p);}return out;}const files=(await walk(root)).filter(f=>f.endsWith('.js')||f.endsWith('.mjs'));const failed=[];for(const file of files){const r=spawnSync(process.execPath,['--check',file],{encoding:'utf8'});if(r.status!==0)failed.push({file:path.relative(root,file),stderr:r.stderr.trim()});}console.log(JSON.stringify({suite:'javascript-syntax',status:failed.length?'FAIL':'PASS',checked:files.length,failed},null,2));if(failed.length)process.exitCode=1;
