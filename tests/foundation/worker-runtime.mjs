import { ensureLocalJsZip,probePipelineRuntimeCapabilities } from '../../pipeline/worker-runtime.js';import { workerSupportsControlledXlsx } from '../../pipeline/pipeline-client.js';
const c=[];const check=(id,ok,note='')=>c.push({id,status:ok?'PASS':'FAIL',note});delete globalThis.JSZip;await ensureLocalJsZip();check('LOCAL_JSZIP_INITIALIZED',Boolean(globalThis.JSZip));const caps=await probePipelineRuntimeCapabilities();check('CAPABILITIES_HAVE_JSZIP',caps.jszip===true);check('DOM_REQUIRED_FOR_XLSX',workerSupportsControlledXlsx({...caps,domParser:false,xlsxIntakeCompatible:false})===false);check('COMPATIBLE_ACCEPTED',workerSupportsControlledXlsx({jszip:true,domParser:true,fileApi:true,crypto:true,xlsxIntakeCompatible:true})===true);const failed=c.filter(x=>x.status==='FAIL');console.log(JSON.stringify({suite:'worker-runtime',status:failed.length?'FAIL':'PASS',checks:c.length,failed:failed.length,capabilities:caps},null,2));if(failed.length)process.exitCode=1;
