import fs from 'node:fs/promises';import path from 'node:path';import {fileURLToPath} from 'node:url';const root=path.resolve(path.dirname(fileURLToPath(import.meta.url)),'../..');const y=await fs.readFile(path.join(root,'.github/workflows/pages.yml'),'utf8');const checks=[];const check=(id,ok)=>checks.push({id,status:ok?'PASS':'FAIL'});check('CONFIGURE_PAGES_V5',/actions\/configure-pages@v5/.test(y));check('UPLOAD_PAGES_ARTIFACT_V4',/actions\/upload-pages-artifact@v4/.test(y));check('DEPLOY_PAGES_V4',/actions\/deploy-pages@v4/.test(y));check('CURATED_SITE_PATH',/path:\s*['"]?\.\/_site['"]?/.test(y));check('BROWSER_ACCEPTANCE_JOB',/browser_acceptance:/.test(y));check('BROWSER_USES_CURATED_SITE',/download-artifact@v4[\s\S]*huf-phase7be-curated-site[\s\S]*path:\s*_site/.test(y));check('DEPLOY_AFTER_BROWSER',/deploy:[\s\S]*needs:\s*browser_acceptance/.test(y));check('PINNED_PLAYWRIGHT',/playwright@1\.55\.0/.test(y));const failed=checks.filter(x=>x.status==='FAIL');console.log(JSON.stringify({suite:'pages-workflow',status:failed.length?'FAIL':'PASS',checks:checks.length,failed:failed.length,details:checks},null,2));if(failed.length)process.exitCode=1;
